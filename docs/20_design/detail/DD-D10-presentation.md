# DD-D10 詳細設計: Presentation（描画・表示用トークン）

- 版: 0.1（起草 cursor-kimi、2026-08-30）
- 上位正本: `docs/10_requirements/要件定義書_検証版_v0.2.md`（REQ-VIS-01..04、REQ-OUT-04、REQ-CON-05）、v0.4 §1.7（形状主・色補助）・付録 C（輪郭表）、v0.5 §1.4（トークン表示専用）、BD-03 §1.2（描画は World を読むだけ）、BD-05 §12.4（snapshot 固定レイアウト）、BD-11 §2（間引き）、BD-08 §9（AT-D10-01/02）
- スコープ: snapshot バッファの消費手順、形状符号の確定、表示用トークンの生成規則、描画間引きの非干渉
- 非スコープ: 画面遷移・スケジューラ（BD-11 / DD-D12）、説明器（DD-D9）、FFI（DD-D11）
- 配置: `app/`（Flutter）。Rust コアの変更なし

## 1. snapshot の消費（確定。REQ-VIS-04、BD-05 §12.4）

- 描画は `kz_snapshot` のコピーアウト後のバッファのみを読む。`step` とバッファを共有しない（BD-11 §5 ADV-15）
- レイアウトは BD-05 §12.4 どおり（ヘッダ 16 B + セル 96 B × 4,096 = 393,232 B）。energy を含まない
- 間引きフレームでは古いバッファを「新しい tick」として描画しない（前フレームの表示を維持する）

## 2. 形状主・色補助の符号表（確定。REQ-VIS-01、v0.4 §1.7・付録 C）

| 系統 | 輪郭（主符号） | 塗り／色（補助） |
|---|---|---|
| アオシキ | 丸みのある塊 | 均一／青緑・中明度 |
| シロナミ | 細長い波型 | 横縞／明るい灰・高明度 |
| アカバエ | 角ばった粒の群れ | 密な点々／橙赤・中高明度 |
| クロシデ | 三日月／ギザ辺の帯 | 粗い斜線／炭色（純黒不可）・低明度 |

- 色を外しても 4 者が形で識別できることが受入条件（REQ-VIS-01 の INSP + USER）
- 環境（空き家ハッチ・waste・carcass）は「場」、系統は「形」として符号系を分ける
- 通常表示は集団パッチ（セルの系統別 biomass を形状パッチの大きさに写像）。重要イベント時のみ局所拡大
- 凡例は色名ではなく形状アイコン＋通称（v0.4 付録 C）

## 3. 表示用トークン（確定。REQ-VIS-03、v0.5 §1.4）

- 最大 8 個、サンプリングは 5 tick ごと、位置はセル中心
- ID = `系統 + セル + tick バケット` で揮発（生涯 ID を持たない）
- 生成は snapshot バッファからの読取のみで、core の PRNG・状態を一切消費しない（REQ-OUT-04）
- 無効化フラグ `tokens_enabled` を config ではなく Presentation 側の表示設定とし、on/off で終了ラベル・state hash がビット一致（AT-D10-01 = D10 の完了判定「トークンを消しても終了ラベルがビット一致」。v0.5 §1.4）

## 4. 描画間引き（確定。REQ-VIS-04、BD-11 §2 #5）

- 間引きは `snapshot`・描画の省略のみ。`step` は上限まで実行し tick を破棄しない
- 間引きの有無はリプレイ経路（save・hash・イベント列）に載らない（AT-D10-02）

## 5. UT / AT 対応

| 検証 | 内容 |
|---|---|
| AT-D10-01 | トークン on/off の同一 seed 2 run で終了ラベル・state hash がビット一致（REQ-OUT-04） |
| AT-D10-02 | 間引きあり/なしで hash 一致 + snapshot バッファが固定レイアウト（REQ-VIS-04） |
| UT-D10-01 | トークン生成: 5 tick ごと・最大 8・ID が tick バケットで揮発（同位置でもバケットが変われば別 ID） |
| UT-D10-02 | snapshot バッファのパース: ヘッダ・セルレコードのオフセットが §12.4 と一致 |
| UT-D10-03 | 形状符号表: 4 系統の輪郭が全て異なる（グレースケール描画で区別可能なことの静的検査） |

## 6. ファイル分割（実装 PR の予定。writer = cursor-grok）

| ファイル | 内容 |
|---|---|
| `app/lib/render/snapshot_view.dart` | §1 のバッファパース（§12.4 レイアウト） |
| `app/lib/render/glyphs.dart` | §2 の形状符号 |
| `app/lib/render/tokens.dart` | §3 トークン生成 |
| `app/test/d10_render_test.dart` | §5 UT |

## 7. 未決事項

なし（D10-Q なし。BD-05 §12.4・v0.4 付録 C・v0.5 §1.4 で必要な値は全て確定済み）
