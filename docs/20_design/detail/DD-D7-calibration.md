# DD-D7 詳細設計: 較正ゲート（4 環境 × 100 seed と上位互換判定）

- 版: 0.1（起草 cursor-kimi、2026-08-30）
- 上位正本: `docs/10_requirements/要件定義書_検証版_v0.2.md`（REQ-OPS-02b、REQ-ACC-04、REQ-GEN-05/06/07、REQ-END-05、REQ-GOAL-03、REQ-SCOPE-06）、v0.5 §1.5・§7.6、`docs/00_product/第3回_検証版計画_v1.0.md` §2.3（打切り基準）、BD-08 §7（AT-D7-01..06）、DD-D6（バッチ基盤）
- スコープ: 較正バッチの実行仕様、統計量の機械的定義、合否判定、manifest、調整ループの規則
- 非スコープ: 煙試験（D6）、代表史の選定と保存（D7 結果を受けた D8/D11 の作業）、パラメータ調整そのもの（本 DD は手順と判定のみ）
- 依存: D6 実装（バッチランナ）マージ後に実装可能

## 1. 較正バッチ（確定）

- コマンド: `sim-cli batch --suite calibration`（DD-D6 のバッチ基盤を流用）
- 構成: 4 環境 × 100 seed、各 2,000 tick。`batch_base = 1_000`（seed = 1000..1099。煙試験の 42..61 と重複させない。初期仮説）
- ペア試験（REQ-GEN-07）: `--suite pair`、carcass_pulse × 100 seed、系統はアカバエ・クロシデのみ（DD-D4 §1 プリセットの id 2,3 の部分集合。D7-Q2）
- 出力: DD-D6 §3 の分布 JSON に加え、10 tick 平均の時系列（系統別総生体量を 10 tick ごとに記録。AT-D7-06 の機械定義が要求）を run レコードに含める

## 2. 統計量の機械的定義（確定）

- **中央値**: 100 値を昇順ソートし `(v[49] + v[50]) / 2`（ゼロ方向丸め）
- **順位**: 環境ごとに系統の終端生体量中央値を降順。同率は id 昇順
- **固定率(L)**: 当該環境の 100 run 中、終了ラベル Fixed かつ勝者 = L の run 数 / 100
- **A 型 / B 型 / C 型**（center_rich の 100 run に対して）: A = Coexist、B = Fixed かつ勝者 = アオシキ、C = Extinct。位置条件は課さない（D7-Q1）
- **出来事 3 種**（REQ-GOAL-03）の機械定義は BD-08 §7 AT-D7-06 の (a)(b)(c) をそのまま採用し、判定に必要な入力は台帳（ReasonCode 集計）・終了ラベル・10 tick 平均時系列・region 栄養（θ = DD-D4 どおり初期中央値の 10%）とする

## 3. 合否判定（確定。REQ-ACC-04）

以下を全て満たすとき D7 通過:

1. **AT-D7-01**: 400 run 全体で 2 種以上の終了ラベルが出現（REQ-SCOPE-06）
2. **AT-D7-02（上位互換。REQ-GEN-05）**: (a) 4 環境中 3 環境以上で中央値 1 位の系統が存在しない、かつ (b) 全環境で中央値 2 位以内かつ固定率 1 位の系統が存在しない
3. **AT-D7-03（ニッチ。REQ-GEN-06）**: 各系統が中央値 1 位になる環境が 1 つ以上
4. **AT-D7-04（分布帯。REQ-END-05）**: center_rich で A ≥ 10%、B ∈ [5%, 20%]、C ≥ 5%
5. **AT-D7-06（出来事。REQ-GOAL-03）**: 3 種が 100 seed 中に各 1 本以上
6. **ペア試験（REQ-GEN-07）**: carcass_pulse での両者合計固定率 = Fixed で終了した run 数 / 100 が 80% 以下（超過時は変換効率を下げる調整対象）

判定は JSON を入力とするスクリプト（`scripts/judge_calibration.py`）が機械的に行い、人の目検を挟まない（REQ-OPS-02b）

## 4. manifest（確定。REQ-OPS-02b、AT-D7-05）

`docs/50_records/calibration/manifest.jsonl` に 1 実行 1 行で追記（append-only）:

```json
{
  "run_id": "cal-2026-08-30-01",
  "git_sha": "<40hex>",
  "config_hash": "<presets + model_version の sha256>",
  "model_version": "d3-v1",
  "batch_base": 1000,
  "verdict": { "at_d7_01": true, "at_d7_02": true, "at_d7_03": false, "at_d7_04": true, "at_d7_06": true, "pair_fixed_rate": 0.41 },
  "distribution": { "center_rich": { "A": 0.12, "B": 0.08, "C": 0.31 } },
  "change_reason": "初回実行（変更なし）"
}
```

`change_reason` は調整時に「動かしたパラメータ群と理由」を必須記録する

## 5. 調整ループの規則（確定。v1.0 §2.3、v0.5 §7.6）

1. 一回の調整で動かすパラメータ群は **一つ**（例: アオシキの intake のみ）。config hash・分布・理由を manifest に保存
2. **勝者補正は禁止**（直前の実行で 1 位だった系統を弱める・敗者を強める方向の調整を理由なく行わない）
3. 許される調整は案 A の軽い戻しに限る（v0.5 §7.6: アオシキ摂取 1.05→1.10、またはアカバエ摂取 1.15→1.10）。B 型 0 本でも案 B へ切替えない
4. 5 日ごとに判断: ①調整継続 ②形質／環境契約を修正して D4 へ戻る ③代表史をモデルの自然な結果へ更新
5. **15 人日を超えたら**代表史 B 型（アオシキ中央固定）を別型へ置換して収束（v1.0 §2.3）

## 6. UT 設計（実数仕様）

| ID | 入力 | 期待 |
|---|---|---|
| UT-D7-01 | 100 値の中央値（偶数個） | (v[49]+v[50])/2 のゼロ方向丸め |
| UT-D7-02 | 中央値同率の 2 系統 | id 昇順で順位決定 |
| UT-D7-03 | 固定率 0.80 ちょうど / 0.81 | 0.80 → 合格 / 0.81 → 不合格（境界） |
| UT-D7-04 | B 型 5% / 4% / 21% | 5% → 帯内 / 4%・21% → 帯外（境界） |
| UT-D7-05 | AT-D7-02(a) ぎりり 2 環境 1 位 / 3 環境 1 位 | 2 → 合格 / 3 → 不合格 |
| UT-D7-06 | manifest.jsonl の各行 | schema 通過・config_hash が presets から再計算一致 |

## 7. AT 対応（BD-08 §7）

| AT | 対応 |
|---|---|
| AT-D7-01..04 | §3 の判定スクリプトが分布 JSON から機械判定 |
| AT-D7-05 | §4 manifest + UT-D7-06 |
| AT-D7-06 | §2 の出来事定義を判定スクリプトに実装（入力: 台帳・ラベル・時系列） |

## 8. 性能

- 400 run + ペア 100 run。DD-D6 §2.4 の目安（1 run ≤ 2 s、4 並列）で ≤ 5 分。CI の定期実行ではなく手動トリガ（workflow_dispatch）とする（初期仮説）

## 9. ファイル分割（実装 PR の予定。writer = cursor-grok）

| ファイル | 内容 |
|---|---|
| `crates/sim-cli/src/batch.rs` | `--suite calibration/pair` と時系列記録の追加（DD-D6 の拡張） |
| `scripts/judge_calibration.py` | §2/§3 の機械判定 |
| `docs/30_contracts/calibration_manifest.schema.json` | §4 schema |
| `crates/sim-cli/tests/d7_calibration.rs` | §6 UT（判定器部分） |

## 10. 未決事項（claude 裁定依頼）

- **D7-Q1**: B 型の機械的定義。「アオシキ中央固定」に位置条件（固定時の分布重心が中央 region）を課すか。推奨: 課さない（Fixed ∧ 勝者 = アオシキ ∧ center_rich のみ）。位置条件は発生率を読めなくし、v0.5 §7.6 の帯（5〜20%）との整合が取れなくなる恐れ
- **D7-Q2**: ペア試験の config。推奨: DD-D4 プリセットの id 2,3 部分集合（他フィールド同一・初期配置は象限中心の id 順 2 セル）。専用プリセットを新設すると REQ-GEN-03「変更は D7 の規則に従う」の管理対象が増える
